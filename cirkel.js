class Cirkel
{
  constructor(position, status)
  {
    this.position = position;
    this.status = status;
  }

  update()
  {
    var xPos = random(window.x)
    var yPos = random(window.y)
  }

  draw()
  {
    
  }
}